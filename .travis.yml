sudo: false
dist: trusty
language: c

addons:
  apt:
    packages:
      - latex-xcolor
      - texlive-base
      - texlive-fonts-extra
      - texlive-fonts-recommended
      - texlive-lang-english
      - texlive-latex-base
      - texlive-latex-extra
      - texlive-latex-recommended

install:
  - make

script:
  - true
